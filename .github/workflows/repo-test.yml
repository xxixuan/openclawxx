name: Repo Dispatch Test

on:
  repository_dispatch:
    types: [test-trigger]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: echo "repository_dispatch works!"
